# Product Registration V6.1 Release 0 Evidence

Captured: 2026-08-20 Asia/Seoul

## Git recovery point

- Integration worktree: `C:\\dev\\yeosonam-os-product-registration-v61`
- Integration branch: `codex/product-registration-v61-authority-20260820`
- V6 base commit: `1e6b4a0da8cef5f17e53ec8fed12f0aae08281d6`
- origin/main at capture: `28fa9f5a19ad6e4bfcc5e22237b938ec234baf22`
- Original dirty worktree: `C:\\dev\\yeosonam-os-product-registration-v6`
- Original dirty branch: `codex/product-registration-engine-v6-20260811`
- Dirty entries at capture: 226

The original worktree was not reset, cleaned, stashed, or edited. Its raw tracked patch and untracked inventory were intentionally excluded from the integration PR because they are non-reproducible workspace artifacts and can contain secret-like evidence. The durable release decision is preserved in this summary and current evidence must be regenerated from a clean release manifest.

## Operational evidence still required before a production write

- Runtime customer-pointer freeze manifest generated by the read-only authority query.
- Current pointer/snapshot/proof export and availability-overlay export.
- Relevant production table logical export or verified backup identifier.
- Manifest hash and query version.
- Runtime count agreement; no expected count is hardcoded.

The historical 11-product audit cohort remains a different evidence set from the runtime freeze manifest and must not be used as its mutation target.

## Gate 1 / clean-build evidence (2026-08-20)

- `git diff --check`: PASS.
- Integration worktree remains intentionally dirty; no merge, reset, stash, production DB, pointer, or deployment mutation was performed.
- Node/npm: `v24.14.0` / `11.9.0`.
- V6.1 migration SHA-256 evidence is captured in `gate1-release-evidence.json`; required artifact hashes are present.
- `npm run build`: PASS (exit code 0). The build compiled successfully, generated 392 static pages, and completed the postbuild output/runtime verification. The build emitted a sitemap warning because no Supabase service-role credentials are configured in this process; no production data was queried or changed.
- The exact-clean-commit build gate remains `NOT_PROVEN`: the candidate changes are uncommitted in this intentionally dirty integration worktree, so a clean commit artifact has not been established.
- Environment inspection found no Supabase/database/preview/browser credentials; values were not printed. Runtime database rehearsal, RLS tests, CAS/fencing concurrency, browser proof, CDN convergence, rollback, gold set, and production pointer freeze remain `NOT_RUN`.
