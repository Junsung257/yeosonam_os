# 실행 계획

1. Pilot readiness 순수 계산과 Codex protocol attestation을 추가한다.
2. platform-admin read-only API로 상태를 노출한다.
3. 운영실에 게이트 표와 다음 조치를 추가한다.
4. `/admin` 진입점·사이드바 링크·권한 경계를 유지한 채 Playwright로 확인한다.
5. 타입·계약·보안·빌드·배포 검증 후에도 Production DB migration은 별도 승인으로 남긴다.
