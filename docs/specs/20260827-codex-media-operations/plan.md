# Codex media operations plan

1. Keep generation, upload, QA, review, and public replacement as separate states.
2. Use only the logged-in Codex built-in ImageGen worker contract.
3. Preserve documentary image boundaries and fail closed on invalid uploads.
4. Verify queue, auth, storage, render, and replacement behavior before activation.
