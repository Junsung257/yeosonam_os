name: SWE-Agent Auto-Fix

on:
  issues:
    types: [labeled]
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to fix'
        required: false

permissions:
  issues: write
  pull-requests: write
  contents: write

jobs:
  auto-fix:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'ai-fix')) ||
      github.event_name == 'workflow_dispatch'

    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Extract issue context
        id: issue
        run: |
          ISSUE_BODY="${{ github.event.issue.body }}"
          ISSUE_TITLE="${{ github.event.issue.title }}"
          ISSUE_NUMBER="${{ github.event.issue.number }}"

          echo "title=$ISSUE_TITLE" >> $GITHUB_OUTPUT
          echo "body=$ISSUE_BODY" >> $GITHUB_OUTPUT
          echo "number=$ISSUE_NUMBER" >> $GITHUB_OUTPUT

      - name: Run TypeScript check
        id: tsc
        continue-on-error: true
        run: npx tsc --noEmit 2>&1 | head -20

      - name: Run ESLint auto-fix
        id: eslint
        continue-on-error: true
        run: |
          npx eslint src --fix --ext .ts,.tsx || true
          git diff --quiet || echo "changes=true" >> $GITHUB_OUTPUT

      - name: Verify fixes
        run: npx tsc --noEmit

      - name: Create PR with fixes
        id: pr
        if: steps.eslint.outputs.changes == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const { execSync } = require('child_process');

            // Create fix branch
            const branchName = `ai-fix/issue-${{ steps.issue.outputs.number }}`;
            execSync(`git checkout -b ${branchName}`);
            execSync(`git config user.name "github-actions"`);
            execSync(`git config user.email "actions@github.com"`);
            execSync(`git add -A`);
            execSync(`git commit -m "fix: auto-fix from issue #${{ steps.issue.outputs.number }}"`);
            execSync(`git push -u origin ${branchName}`);

            // Create PR
            const { data: pr } = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `fix: auto-fix for "${{ steps.issue.outputs.title }}" (#${{ steps.issue.outputs.number }})`,
              body: `Automatic fix for issue #${{ steps.issue.outputs.number }}\n\n**Original Issue:**\n${{ steps.issue.outputs.body }}`,
              head: branchName,
              base: context.ref
            });

            console.log(`Created PR #${pr.number}`);
            core.setOutput('pr_number', pr.number);

      - name: Update issue labels
        if: steps.pr.outputs.pr_number
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.issue.outputs.number }},
              labels: ['ai-fix-completed']
            });

            github.rest.issues.removeLabel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.issue.outputs.number }},
              name: 'ai-fix'
            });

            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.issue.outputs.number }},
              body: `✅ Auto-fix PR created: #${{ steps.pr.outputs.pr_number }}`
            });

      - name: Comment on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.issue.outputs.number }},
              body: `❌ Auto-fix failed. Manual review needed.\n\n\`\`\`\n${{ steps.tsc.outputs.message }}\n\`\`\``
            });
