import PartnerPreviewClient from './PartnerPreviewClient';

export const metadata = {
  title: '파트너 프론트 미리보기 | Admin',
};

export default function PartnerPreviewPage() {
  return <PartnerPreviewClient />;
}
