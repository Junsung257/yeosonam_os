node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Partytown — `postinstall` / `npm run partytown:copy` 가 node_modules 에서 복사 (원본은 커밋 안 함)
/public/~partytown/

# Partytown — postinstall(`copy-partytown-lib.cjs`)로 생성, 레포에 커밋하지 않음
public/~partytown/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local

# Vercel env pull 결과 (로컬 개발용, git에 커밋 금지)
.env.vercel

# Google Service Account 키 / 기타 cloud credentials — 절대 git에 들어가면 안 됨
*-service-account*.json
service-account*.json
gen-lang-client-*.json
google-credentials*.json
*credentials*.json
*.serviceaccount.json

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts
.mcp.json

# 임시 테스트 파일 (어셈블러/파싱 결과)
temp_*
temp_out.json
temp_pkg.json
temp_qingdao*.json
temp_qingdao.txt

# 로컬 백업 폴더
백업/

# Supabase CLI 자동생성
supabase/.temp/

# 스크래치/실험 폴더 (LLM 출력 덤프, 실험 HTML, 임시 로그)
scratch/

# Playwright 실패 덤프 (재현용)
test-results/

# Claude Code 런타임 산출물
.claude/scheduled_tasks.lock
.claude/settings.local.json

# 외부 사이트 클립 / 임시 텍스트
여행약관.txt

# Local logs/reports generated during audits/dev
.ai_rewrite.log
.next-dev.log
.pipeline.log
.pipeline2.log
.sync_all.log
.sync_expand.log
.tsc.log
lint_output.txt
.cursor-home-fetch.html

# Local temporary audit/export artifacts
tmp-*.json
tmp-*.txt
tmp-*.html

# Lighthouse 로컬 분석 산출물
.tmp-lh-*.json

# Serwist/next-pwa 빌드 산출물 (빌드 시 자동 생성)
/public/sw.js
/public/swe-worker-*.js

# Lazyweb 디자인 리포트 (로컬 분석 전용)
.lazyweb/

# Page audit 산출물 (test infra만 commit, 결과 + 인증 상태는 로컬 전용)
tests/audit/results*.ndjson
tests/audit/results*.json
tests/audit/run.log
tests/audit/auth.json

# 임시 디버그 텍스트 dump
.tmp-detail-html.txt

# 로컬 audit/report 산출물 (CI 또는 수동 실행 결과, 커밋 X)
api-contract-report.json
db-query-report.json
migration-safety-report.json

# 브리핑 PPT 생성 결과 (로컬 산출물, public/ 에 자동 복사돼도 커밋 X)
/public/briefings/*.html
/public/briefings/*.json
/public/briefings/*.pptx

# Pretendard 폰트 (postinstall 로 node_modules 에서 복사, 커밋 X)
/public/fonts/pretendard/

# 사용자 생성 PPT/브리핑 산출물 (한글 파일명, 루트에 떨어진 것)
*.pptx
/창*.pptx
/청*.json

# 2026-05-18 박제 (feedback_session_2026_05_17_18_self_review §11): 1회용 진단/디버그 스크립트.
# 영구 박제할 backfill·migration 만 명시적으로 추가. nodemon 스타일 디버그는 커밋 X.
db/debug_*.ts
db/debug_*.js
db/check_*.ts
db/check_*.js
db/diagnose_*.ts
db/diagnose_*.js
db/fix_*.ts
db/fix_*.js
db/test_*.ts
db/test_*.js
db/verify_*.ts
db/verify_*.js
db/create_*.mjs
db/create_*.sql
db/force_*.ts
db/force_*.js
db/rerun_*.ts
db/cleanup_*.js
db/fill_*.js
db/reenrich_*.js
db/final_audit.ts
db/session_audit_*.ts
db/bulk_backfill_*.ts
db/bulk_resolve_*.ts
db/backfill_*.ts
db/test_set_*.ts
# 스크립트 — 서비스 계정 키 / OAuth 시크릿 (절대 git에 들어가면 안 됨)
scripts/google-sa-value.txt
scripts/*sa-value*.txt
scripts/oauth-*.mjs
scripts/*client-secret*.mjs
scripts/gsc-owner-delegate.html

dev-server.log
.gstack/

.vercel
.env*.local

# 세션 작업 파일 (git에 포함 금지)
.commit-msg.txt
PR_BODY.md
PLAN.md
fix-catch-patterns.py

# Codex local workspace metadata
.agents/
.codex/
.codex-*.err.log
.codex-*.out.log
.codex-*.pid
.codex-*.mjs
